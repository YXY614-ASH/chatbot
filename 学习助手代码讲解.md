# AI 学习助手 —— 代码逐行讲解

> 你的第一个 LLM 应用。别看它短,它包含了「AI 应用开发」的三大核心动作:
> **① 拿到钥匙连接模型 → ② 写一个对话函数 → ③ 起一个网页界面**。
> 下面把每一行都讲清楚,你照着理解一遍,就入门了。

---

## 一、完整代码

```python
# -*- coding: utf-8 -*-
"""
AI 学习助手 —— 你的第一个 LLM 应用（MVP）
功能：一个能多轮对话的网页聊天机器人，背后是 DeepSeek 大模型
"""

import os
from openai import OpenAI
import gradio as gr

# ============ 1. 配置 API key ============
API_KEY = "sk-你的key"   # ← 这里填你自己的 key

client = OpenAI(
    api_key=API_KEY,
    base_url="https://api.deepseek.com",  # DeepSeek 兼容 OpenAI 接口
)

# ============ 2. 对话函数（核心） ============
def chat(message, history):
    messages = [{"role": "system", "content": "你是吴佳诣的学习助手，回答简洁、准确、用中文。"}]
    for user_msg, bot_msg in history:
        messages.append({"role": "user", "content": user_msg})
        messages.append({"role": "assistant", "content": bot_msg})
    messages.append({"role": "user", "content": message})

    resp = client.chat.completions.create(
        model="deepseek-chat",
        messages=messages,
        temperature=0.7,
    )
    return resp.choices[0].message.content

# ============ 3. 启动网页界面 ============
gr.ChatInterface(
    fn=chat,
    title="AI 学习助手",
    description="试试问我：「用通俗的话解释什么是过拟合」或「帮我规划三极管的学习」",
).launch()
```

---

## 二、分块讲解

### 第 1 块：导入库

```python
import os
from openai import OpenAI
import gradio as gr
```

| 代码 | 作用 |
|---|---|
| `import os` | 操作系统相关工具(读环境变量、处理文件等)。这个程序暂时没直接用到,留着备用 |
| `from openai import OpenAI` | 引入 `OpenAI` 这个类,它负责「和模型对话」。注意:虽然叫 OpenAI,但 DeepSeek 兼容它,所以能用 |
| `import gradio as gr` | 引入 Gradio,负责「做网页界面」,不用自己写 HTML |

---

### 第 2 块：配置 key 和客户端

```python
API_KEY = "sk-你的key"

client = OpenAI(
    api_key=API_KEY,
    base_url="https://api.deepseek.com",
)
```

- **`API_KEY`**:相当于你的「钥匙/账号密码」,证明你有权调用 DeepSeek 的模型。DeepSeek 靠它计费。
- **`client = OpenAI(...)`**:创建一个「客户端」对象。之后所有对话都通过 `client` 发出去。
- **`base_url` 是这一块的关键**:`OpenAI` 默认连的是 OpenAI 官方服务器,这里把它改成 DeepSeek 的地址,所以同样一套代码就能用 DeepSeek 的模型。这就是「兼容接口」的含义——**一个标准,多家公司通用**。

> ⚠️ 安全:key 就是钱。这个文件别发给别人、别传到公开的 git 仓库。

---

### 第 3 块：对话函数(整个程序的核心)

```python
def chat(message, history):
    messages = [{"role": "system", "content": "你是吴佳诣的学习助手，回答简洁、准确、用中文。"}]
    for user_msg, bot_msg in history:
        messages.append({"role": "user", "content": user_msg})
        messages.append({"role": "assistant", "content": bot_msg})
    messages.append({"role": "user", "content": message})

    resp = client.chat.completions.create(
        model="deepseek-chat",
        messages=messages,
        temperature=0.7,
    )
    return resp.choices[0].message.content
```

两个参数是 Gradio **自动传进来的**:
- `message` — 用户**刚刚输入**的那句话
- `history` — **之前的对话记录**,格式是一串 `(用户说的, 机器人答的)` 元组

逐步看:

1. **`messages = [...]`** — 先放一条 `system` 消息,这是「人设/规则」:告诉模型它是谁、要什么风格。改这句话就能改助手的性格。

2. **`for user_msg, bot_msg in history`** — 把历史对话一条条搬进 `messages`,并且严格按 `user` → `assistant` → `user` → `assistant` 的顺序排列(模型要求这个格式)。

3. **`messages.append(...)`** — 最后把用户这次的新问题加进去。这时 `messages` 就是「完整对话剧本」。

4. **`client.chat.completions.create(...)`** — 真正发请求给模型:
   - `model="deepseek-chat"`:用哪个模型
   - `messages=messages`:把剧本交给它
   - `temperature=0.7`:回答的随机程度

5. **`return resp.choices[0].message.content`** — 从返回结果里,一路取到「模型回答的那段文字」,返回给界面显示。

> **为什么要把历史重新拼一遍?** 因为大模型本身**没有记忆**,每次请求都是全新的、彼此独立的。它不知道你上一句说了什么。所以我们必须每次把「历史 + 新问题」完整地再发一遍,它才能「看起来记得上下文」。

---

### 第 4 块：启动网页

```python
gr.ChatInterface(
    fn=chat,
    title="AI 学习助手",
    description="...",
).launch()
```

- `gr.ChatInterface(...)`:Gradio 提供的**现成聊天界面**,自带输入框、气泡、历史记录,不用自己画。
- `fn=chat`:告诉它「用户每发一句话,就调用 `chat` 这个函数」。
- `.launch()`:启动服务器,默认监听本机 `http://127.0.0.1:7860`。

---

## 三、三个关键概念(面试/进阶都会用到)

1. **system prompt(系统设定)**:`messages` 里第一条 `system` 消息,定义模型的角色和行为。这是「提示工程」的核心——**同样一个模型,改这一句话,能变成客服、老师、翻译、代码助手**。

2. **temperature(温度)**:控制随机性。
   - 接近 `0`:回答严谨、固定,适合答题、提取信息
   - 接近 `1`:回答发散、有创意,适合写作、聊天

3. **多轮对话 = 历史拼接**:模型失忆,靠你把历史反复重传。**副作用**:对话越长,每次发的字越多,花的时间/钱越多。后面学 RAG 和「记忆压缩」就是为了解决这个。

---

## 四、可以自己动手改的三个小实验

1. 把 `system` 那段话改成「你是一个幽默的段子手」,再问它问题,看风格变化。
2. 把 `temperature` 改成 `0.2`,问同一个问题,和 `0.7` 对比回答的稳定性。
3. 把 `title` 和 `description` 改成你自己的话,重启程序看界面变化。
